# Landing Page (Figma-match)

## Run

- Open `index.html` in your browser, or
- Use any static server (for example VS Code / Cursor “Live Server”).

## Assets

- Exported from Figma into `assets/` as SVG/PNG.
